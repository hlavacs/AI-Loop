Connect approval to a real Start Implementation GUI action.

Enforce blocking automated coverage for mandatory and high-risk requirements.

Require runtime proof that the intended verification case actually executed.

Repair structured evidence editing.

Publish formal and retarget tasks through the normal controller queue.

Harden subprocess isolation and output limits.

Add configurable evidence adapters and task-scoped prompts.

Add one genuine GUI-to-completion integration test.

Improve user experience. Now the user in the specification wizard sees a lot of empty text fields and has no clue how this works, what he is supposed to do, how to act in the process. We need a full user experience, with enough information in the GUI to understand the whole process and each information thaht the user should input and how they are connected and how the process later on works, meaningful examples, saving and reloading the specification, acting like a real helpful wizard. The wizard shoukd also immediately evaluate user input and give feedback on the semantics. User should have a test button with which the wizard analyses the total input and suggests improvements.

