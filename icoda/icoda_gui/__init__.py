"""Tk widgets of ICODA that are larger than the main window itself: the specification editor and, later, the
provider field and the proposal panel. Everything here depends on ``icoda_core`` and on tkinter only."""
